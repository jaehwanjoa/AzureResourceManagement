name: manage-azure-policy-4dacd85f
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'policies/Azure_Security_Center를_모니터링하려면_가상_머신에_Log_Analytics_에이전트를_설치해야_함_a4fe33eb-e377-4efb-ab31-0784311bc499/**'
#      - .github/workflows/manage-azure-policy-4dacd85f.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_4dacd85f}}
        allow-no-subscriptions: true
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          policies/Azure_Security_Center를_모니터링하려면_가상_머신에_Log_Analytics_에이전트를_설치해야_함_a4fe33eb-e377-4efb-ab31-0784311bc499/**
